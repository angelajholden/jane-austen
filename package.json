{
  "name": "jane-austen-database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Express and SQLite scaffold for the Jane Austen Database.",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "db:init": "node scripts/init-db.js",
    "import": "node scripts/import-books.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^5.2.1"
  }
}
